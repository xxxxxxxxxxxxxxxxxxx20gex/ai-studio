# AGENTS.md

> 这个文件给以后任何协助维护本知识库的 AI 协作者（包括未来的我）看。
> 目的：让每次新增内容都按统一规则走，不重复造轮子、不丢上下文。

---

## 1. 这是什么库

李永琪的**面试准备知识库**，服务方向：**Agent 应用开发**（应用层，非纯算法）。
核心用户画像：硕士 CV 背景转大模型应用，有生产级项目经验，正在面试。
所有内容最终都要回答一个问题：**面试时怎么说出口？**

不是读书笔记仓库，不是技术文档站。每篇外部内容都要转成"我能用的弹药"。

---

## 2. 目录结构

```
面试知识库/
├── README.md          # 总索引（唯一入口）
├── AGENTS.md          # 本文件
├── 00_我的话术/       # 自己的东西，面试前必看
│   ├── 高频问题库.md
│   ├── 自我介绍.md
│   ├── 项目STAR.md
│   └── 反问清单.md
├── 01_业务认知/       # 外部内容：博主视频/文章
│   └── YYYY-MM-DD_来源_主题/
│       ├── 原文.md
│       ├── 提炼.md
│       └── 问答.md
├── 02_技术八股/       # 技术复习材料（来自 ai-studio）
├── 03_面试复盘/       # 每场面试后补一笔
│   └── YYYY-MM-DD_公司_轮次.md
└── assets/            # 截图、音频等附件
```

---

## 3. 新增一篇外部内容的标准 SOP

当用户丢来一个视频链接或文章，按此执行，除非用户明确说只要原文：

1. **原文保管**：用视频/文本提取工具拿到逐字稿或原文，一字不删存到 `原文.md`。文件头必须写：来源、链接、时长/字数、收录日期、标签。
2. **提炼**：写 `提炼.md`，**不超过 5 条**，每条一句话，是扫一眼能记住的结论。
3. **问答**：写 `问答.md`，出 5-8 个面试可能被问的问题，每个问题给"骨架 + 我的话"两段。**"我的话"里必须挂到用户自己的真实项目**（WujieAI 三代演进 / 多模型网关 / 房产客服 Agent / CV 转 Agent），不能只复述博主观点。
4. **更新索引**：在 README.md 的"01 业务认知"表格里加一行。
5. **有新的通用问题**，顺手补进 `00_我的话术/高频问题库.md`。

### 命名规则
`YYYY-MM-DD_来源博主_主题/`，例如 `2026-09-18_XX博主_Agent落地踩坑/`。
- 日期用内容收录当天；
- 来源写博主/作者名，不写平台名（B站在原文.md里注明即可）；
- 主题短，5-10 个字。

---

## 4. 写作原则（硬性要求）

1. **先说结论，再展开**。每个文件开头给一句话核心，不要让面试官/用户读半天才抓到重点。
2. **回答必须挂自己项目**。博主讲的是别人的道理，用户要的是"我怎么用这个道理包装自己"。
3. **数字要有口径**。简历和话术中的关键数字（31.2 万请求、80%、1k+ 用户、4 所高校）要在 `项目STAR.md` 里标注统计窗口和口径，避免面试被追问时露馅。
4. **贡献边界要诚实**。"百年数学问题证明"是团队成果，用户负责平台和链路，绝不能写成"我证明了"。
5. **别写 AI 味的废话**。不要"在当今快速发展的 AI 时代……"这种开头。直接给能用的话。
6. **每个回答都能 30 秒内说完**。写之前假设用户在面试现场只有 30 秒。

---

## 5. 面试后的动作

用户面完试，主动问一句"今天被问了什么、哪里卡壳"，然后：
1. 在 `03_面试复盘/` 新建或更新当天文件，记录：被问的问题、答得好的、卡壳的、观察到的信号（比如问"愿不愿意顶产品活"= 团队小缺人）。
2. 新出现的通用问题，补进 `00_我的话术/高频问题库.md`。
3. 如果用户提到了新的薄弱点，在对应目录加 TODO。

---

## 6. 不要做的事

- 不要把外部内容大段粘贴到 `00_我的话术/`——那里只放用户自己的话。
- 不要在没经过用户确认时改写他的简历事实、项目数字、贡献描述。
- 不要新建 README 之外的索引文件——所有索引走 README.md。
- 不要把 `02_技术八股/` 里的笔记当最终答案，那是参考材料；面试要用自己的话讲。
- 用户没要求时，不要主动扩充技术八股内容。

---

## 7. 当前状态快照（2026-09-16）

- 方向：Agent 应用开发
- 工作：杭州超节点（2025.11 至今），之前 Nestflo（2025.03-11）
- 教育：大连民族大学计算机技术硕士（2026.06 毕业），研究方向视频行为识别，两篇 PRCV（CCF-C）
- 核心项目：WujieAI 数学科研 Agent（三代演进）、多模型 AI 网关、Nestflo 房产客服 Agent
- 待补话术：为什么从 CV 转 Agent、跳槽动机、缺点、最大困难
